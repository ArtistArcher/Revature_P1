@model IEnumerable<ModelLayer.ViewModels.InventoryViewModel>

@{
    ViewData["Title"] = $"Melf's Magic - Shop Name.";
}

<h1>ShowLocationInventory</h1>

<p>
    @*<a asp-action="Create">Create New</a>*@
    model.Name
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.InventoryId)*@
                Store
            </th>
            <th>
            @*@Html.DisplayNameFor(model => model.LocationId)*@
            </th>
            <th>
            @*@Html.DisplayNameFor(model => model.ProductId)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @*@Html.DisplayFor(modelItem => item.InventoryId)*@
                @Html.DisplayFor(modelItem => item.StoreName)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.LocationId)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.ProductId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                $@Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-controller="Cart"
                   asp-action="AddProductToCart"
                   asp-route-id="@item.ProductId">Purchase</a>  @*|

        @Html.ActionLink("Purchase", "AddProductToCart", new { id = item.ProductId })  |
                |
        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
            </td>
        </tr>
} <!-- END FOREACH -->
    </tbody>
</table>
